# TEMPLATE ONLY: replace every placeholder and apply a private copy manually.
# This file is intentionally excluded from kustomization.yaml.
apiVersion: v1
kind: Secret
metadata:
  name: supplychain-secrets
  namespace: supplychain
type: Opaque
stringData:
  KAFKA_BOOTSTRAP_SERVERS: "REPLACE_WITH_CONFLUENT_BOOTSTRAP_SERVER"
  KAFKA_SASL_USERNAME: "REPLACE_WITH_CONFLUENT_API_KEY"
  KAFKA_SASL_PASSWORD: "REPLACE_WITH_CONFLUENT_API_SECRET"
  DATABASE_URL: "postgresql+psycopg2://REPLACE_USER:REPLACE_PASSWORD@REPLACE_HOST:5432/supplychain"
  AISSTREAM_API_KEY: "REPLACE_WITH_AISSTREAM_KEY_OR_LEAVE_EMPTY_FOR_MOCK"
  OTEL_EXPORTER_OTLP_ENDPOINT: ""
